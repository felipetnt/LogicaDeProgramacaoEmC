// Faça um algoritmo que leia dois números inteiros positivos (Num1 e Num2) e 
// imprima o quociente (Quoc) e o resto (Resto) da divisão de Num1 por Num2, 
// utilizando apenas as operações de adição e subtração. 
// Ex.: N1=10; N2=2 => Q=5 R=0 
//Entrada: Num1, Num2 informados pelo usario.
//Processador: Dividir num1 por num2, utilizando somente +, -.
//Saida: Apresentar o quociente e o resto.

#include <stdio.h>
int main(){
    int num1;
    int num2;
    int quoc;
    int rest;
    int contador;

    printf("Digite o numero que sera o dividendo da divisao: \n");
    scanf("%d", &num1);
    printf("Digite o numero que sera o divisor da divisao: \n");
    scanf("%d", &num2);

    while(num2 <= num1){
        
    }
    
}